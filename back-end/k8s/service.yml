apiVersion: v1
kind: Service
metadata:
  name: vr-match-back-end
spec:
  selector:
    app: vr-match-back-end
  type: NodePort
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8080
      name: http
